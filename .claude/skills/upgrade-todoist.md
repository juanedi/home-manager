---
description: Upgrade Todoist to the latest version
user_invocable: true
---

Run `make upgrade-todoist` from the project root to fetch the latest Todoist AppImage version and update `programs/todoist/sources.nix`.

After the script completes, offer to run `make switch` to apply the new version.
